Sources for the plugin `plugin.appodeal.beta.OpenX`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.OpenX" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
